name: flutter_responsive_framework_tutorial
description: A new Flutter project.
publish_to: 'none' 
environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

  cupertino_icons: ^1.0.6
  responsive_framework: ^0.1.4
  freezed_annotation: 1.1.0
  get_it:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  injectable_generator: 1.5.2

flutter:
  uses-material-design: true

  assets:
    - assets/